# 后端服务 + 基础服务重构方案（粗粒度进程）

> **版本**：v4.1.1（2026-08-04）  
> **状态**：**PR-A～PR-D 已实施 + 审查补齐**  
> **对齐**：[后端架构规划.md](./后端架构规划.md)（逻辑层 Lxx / 模块 Mxx 仍为真相源）  
> **相对 v3 修订**：  
> - **不按逻辑域拆成 12 个 Go 微服务**  
> - **同类能力合并为一个部署单元、一个端口**  
> - 逻辑模块（platform / policy / …）保留为进程内边界 + 六边形包，便于以后按需再拆  

---

## 0. 定稿原则

| # | 原则 |
|---|---|
| 1 | **粗粒度优先**：一类能力 = 一个进程 = 一个对外端口 |
| 2 | **逻辑可细、进程要粗**：`platform/policy/audit…` 是模块，不是独立微服务 |
| 3 | **Go 六边形**：每个部署单元内按 Ports & Adapters；模块间禁止跨包直连表 |
| 4 | **Python 执行面独立**：推理 / RAG / 沙箱各一端口（隔离要求） |
| 5 | **基础服务分目录**：数据、IdP、策略引擎、观测各自可编排 |
| 6 | **永不混部**：`de-skill-runtime` · `de-agent-runtime` · `de-rag`（执行面互不混） |

---

## 1. 部署单元总览（目标态）

相对「12 个 Go 微服务」，收敛为 **5 个 Go 进程 + 3 个 Python 进程 + 1 个网关**：

```text
Callers
   │
   ▼
┌────────────┐
│ de-gateway │  :8088 / :8443     Envoy（api + agent 入口）
└─────┬──────┘
      │
      ├──────────────┬──────────────────┬─────────────────┐
      ▼              ▼                  ▼                 ▼
┌──────────┐  ┌──────────┐      ┌───────────┐     ┌────────────┐
│ de-sys   │  │ de-collab│      │ de-cap    │     │ de-workflow│
│  :8100   │  │  :8101   │      │  :8102    │     │  :8103     │
│ 地基+运营 │  │ 协作编排 │      │ 能力控制面 │     │ 流程+Worker│
└────┬─────┘  └────┬─────┘      └─────┬─────┘     └──────┬─────┘
     │             │                  │                  │
     │             └────────┬─────────┘                  │
     │                      ▼                            │
     │             ┌────────────────┐                    │
     │             │ de-agent-      │ :8091 FastAPI      │
     │             │ runtime        │◄───────────────────┘
     │             └───────┬────────┘
     │         ┌───────────┼───────────┐
     │         ▼           ▼           ▼
     │   de-rag:8092  de-skill-runtime:8093  （+ Temporal via workflow）
     │
     ▼
  infra / obs（见 §4）
```

| 部署单元 | 端口 | 栈 | 内含逻辑模块（原细拆名） | 逻辑层 |
|----------|------|----|--------------------------|--------|
| **de-gateway** | 8088/8443 | Envoy | 统一入口、TLS、路由、限流 | L00·L01 |
| **de-sys** | **8100** | Go 六边形 | **platform · policy · audit · ops** | L10·L11·L12·L13 |
| **de-collab** | **8101** | Go 六边形 | **collab · employee** | L02 |
| **de-cap** | **8102** | Go 六边形 | **model · knowledge · memory · skill · channel** | L04–L07·L09 |
| **de-workflow** | **8103** | Go 六边形 | **workflow**（HTTP 控制面 + Temporal Worker） | L08 |
| **de-agent-runtime** | **8091** | FastAPI | LangGraph / 流式 / 工具编排 | L03 |
| **de-rag** | **8092** | FastAPI | 切片 / Embedding / Milvus / Rerank | L05 |
| **de-skill-runtime** | **8093** | FastAPI + 隔离网 | Skill / MCP 沙箱执行 | L07 |

> **合计业务部署单元**：8（+ gateway）= 9。  
> **过渡**：现有 `de-core:8080` 作兼容壳，切流完成后删除。现有独立进程 `de-policy:8094`、`de-audit:8095` 在 P2 并入 **de-sys**（路径由 gateway 改写）。

### 1.1 为何这样合并

| 单元 | 合并理由 |
|------|----------|
| **de-sys** | 地基与合规读写同命运：租户/配额、策略评估、审计写入、运营只读聚合；共享工作区上下文与鉴权 |
| **de-collab** | 会话/任务与岗位装配强耦合（发消息 → 解析员工绑定 → 调能力） |
| **de-cap** | 能力控制面同类：模型/知识/记忆/技能目录/渠道，均为「配置 + 发布引用」，非执行 |
| **de-workflow** | 依赖 Temporal Server；Worker 长驻，与纯 CRUD 控制面生命周期不同，单独一端口 |
| **三个 Python** | 资源、安全、扩缩模型不同；skill-runtime 必须隔离网 |

### 1.2 明确不做的细拆

以下**不再**各自占用独立端口 / 独立微服务：

`de-platform` · `de-policy` · `de-audit` · `de-ops` · `de-employee` · `de-model` · `de-knowledge` · `de-memory` · `de-skill` · `de-channel`

它们降级为 **进程内模块名**（包路径 / schema 前缀 / proto package），API 路径可继续按域划分（如 `/api/models/*`），但由同一进程监听。

---

## 2. 进程内模块与六边形

### 2.1 每个 Go 部署单元目录

```text
services/de-sys/
├── SERVICE.md
├── Dockerfile
├── go.mod
├── cmd/de-sys/main.go              # Composition Root：一个端口
└── internal/
    ├── domain/
    │   ├── platform/               # 模块实体
    │   ├── policy/
    │   ├── audit/
    │   ├── ops/
    │   └── port/                   # 跨模块端口接口
    ├── application/
    │   ├── platform/
    │   ├── policy/
    │   ├── audit/
    │   └── ops/
    └── adapters/
        ├── inbound/http/           # 按模块分子路由，同 Listen
        └── outbound/
            ├── postgres/
            ├── redis/
            ├── opa/                # de-sys 调 infra-opa
            ├── kafka/
            └── opensearch/
```

`de-collab` / `de-cap` / `de-workflow` 同构，仅模块集合不同。

### 2.2 依赖规则

| 层 | 可依赖 | 禁止 |
|----|--------|------|
| `domain/<module>` | 标准库 | adapter、SQL、HTTP |
| `application/<module>` | 本进程 domain/port | 其他部署单元的 internal |
| 跨模块（同进程） | 经 `domain/port` 或 application 门面 | 直接读写对方表 |
| 跨进程 | HTTP/Connect/事件 | 直连对方 schema |

### 2.3 路由归属（对外仍按域，对内一个端口）

| 部署单元 | 示例路径前缀（对齐现有 Mock） |
|----------|-------------------------------|
| de-sys | `/api/workspaces` · `/api/settings` · `/api/policy/*` · `/api/zero-trust/*` · `/api/audit/*` · `/api/ops/*` |
| de-collab | `/api/sessions` · `/api/tasks` · `/api/employees` · `/api/copilot/*` |
| de-cap | `/api/models` · `/api/knowledge` · `/api/memory` · `/api/skills` · `/api/channels` · `/api/channel-control/*` |
| de-workflow | `/api/workflows` · Temporal Worker（非 HTTP） |
| de-agent-runtime | `/v1/agent/*`（经 gateway `/agent`） |
| de-rag | `/v1/rag/*` |
| de-skill-runtime | `/v1/skills/execute` 等 |

Gateway 按路径前缀转发到上述 **4 个 Go 端口 + 3 个 Python 端口**。

---

## 3. 仓库目录

```text
backend/
├── README.md
├── go.work
├── proto/
│   └── de/{platform,policy,audit,ops,collab,employee,
│            model,knowledge,memory,skill,channel,workflow,rag,runtime}/v1/
├── libs/
│   ├── errors/ · response/ · auth/ · infra/
│   ├── hexkit/
│   └── clients/                    # 调其他部署单元的薄客户端（可选）
├── services/                       # ★ 仅「部署单元」一级目录
│   ├── de-gateway/
│   ├── de-sys/                     # :8100
│   ├── de-collab/                  # :8101
│   ├── de-cap/                     # :8102
│   ├── de-workflow/                # :8103
│   ├── de-agent-runtime/           # :8091 FastAPI
│   ├── de-rag/                     # :8092
│   ├── de-skill-runtime/           # :8093
│   └── de-core/                    # 过渡壳 :8080 → 删除
├── infra/                          # 基础服务（§4）
├── obs/
├── deploy/
│   ├── compose.yml                 # include 各 snippet
│   └── networks.md
└── scripts/
    ├── new-go-service.sh           # 生成粗粒度六边形模板
    └── gen-proto.sh
```

---

## 4. 基础服务（同样一类一编排单元）

不拆业务细微服务，但基础组件仍独立目录、独立升级：

| 目录 | 组件 | 默认端口 | 主要消费者 |
|------|------|----------|------------|
| `infra/postgres` | PostgreSQL 16 | 5432 | de-sys / collab / cap / workflow |
| `infra/redis` | Redis 7 | 6379 | de-sys / collab |
| `infra/vault` | Vault | 8200 | de-cap（凭据引用） |
| `infra/kafka` | Redpanda/Kafka | 9092 | de-sys(audit) / de-cap(channel) |
| `infra/temporal` | Temporal | 7233 | de-workflow |
| `infra/opa` | OPA | 8181 | de-sys(policy) |
| `infra/opensearch` | OpenSearch | 9200 | de-sys(audit 检索) |
| `infra/milvus` | Milvus(+etcd/minio) | 19530 | de-rag |
| `infra/object-storage` | MinIO | 9000 | 知识原件 / 导出 |
| `infra/authentik` | Authentik | 9000/9443 | OIDC |
| `infra/dex` | Dex（可选开发） | 5556 | 本地 OIDC |
| `infra/spire` | SPIRE（生产） | — | mTLS |
| `obs/prometheus` | Prometheus | 9090 | 全站 |
| `obs/grafana` | Grafana | 3000 | 看板 |
| `obs/otel-collector` | OTel Collector | 4317/4318 | 链路 |

**库内分 schema（同库可分实例）**：`platform` · `policy` · `audit` · `ops` · `collab` · `employee` · `model` · `knowledge` · `memory` · `skill` · `channel` · `workflow` —— 所有权跟逻辑模块，不跟「细微服务」。

---

## 5. Schema / 数据所有权（按模块，跨进程禁止直连）

| 写所有者（模块） | 所在进程 | Schema 前缀 |
|------------------|----------|-------------|
| platform / policy / audit / ops | **de-sys** | `platform.*` `audit.*` … |
| collab / employee | **de-collab** | `collab.*` `employee.*` |
| model / knowledge / memory / skill / channel | **de-cap** | `model.*` … `channel.*` |
| workflow | **de-workflow** | `workflow.*` + Temporal |

跨进程只走 API / 事件。同进程跨模块经 port，不互相 `SELECT` 对方表（便于日后拆分）。

---

## 6. 关键调用链

```text
Callers
  → de-gateway
  → de-collab          （会话 / 任务 / 员工绑定）
  → de-sys             （policy 评估；异步 audit）
  → de-cap             （model 路由解析等）
  → de-agent-runtime
       ├─ de-rag
       ├─ de-cap（memory / skill 元数据）
       ├─ de-skill-runtime
       └─ de-workflow
  → de-collab 回写证据
  → de-cap（channel 可选出站）
  ··异步··→ de-sys（audit / 用量）
```

---

## 7. 网络

| 网络 | 成员 |
|------|------|
| `de_net` | gateway、de-sys、de-collab、de-cap、de-workflow、agent-runtime、rag、基础数据面 |
| `de_exec_net` | **仅** de-skill-runtime |
| `de_obs_net`（可选） | prometheus / grafana / otel |

---

## 8. 分期实施

| 阶段 | 内容 | 结果 |
|------|------|------|
| **P0 / PR-A** | `services/` + `infra/` + `obs/` + `libs/hexkit`；`ServiceMode`；cmd 归位 | **已完成** |
| **P1 / PR-B** | Python → FastAPI（`services/de-*-runtime`） | **已完成** |
| **P2 / PR-C** | **de-sys + de-collab** 切流；`envoy.coarse.yaml` | **已完成** |
| **P3–P4 / PR-D** | **de-cap + de-workflow**；coarse 默认不启 8094/8095 | **已完成** |

后续增强（非阻塞）：六边形包从 `internal/server` **物理迁入** `services/*/internal/{domain,application,adapters}`（骨架已就位）；跨进程强一致事件；按 `services/de-core/RETIRE.md` 退役兼容壳。

### 审查补齐（v4.1.1）

| 项 | 状态 |
|----|------|
| `/api/channel` 归属 de-cap | ✅ |
| Envoy cluster → Docker DNS（de-sys 等） | ✅ |
| Connect 按 mode 挂载 + Envoy `/de.collab.` 等 | ✅ |
| de-workflow 注入 runtime/rag URL | ✅ |
| `DE_SERVICE` 环境变量 | ✅ |
| `/api/governance` `/api/audit` 别名 | ✅ |
| metrics `service` label + prometheus 8100–8103 | ✅ |
| mode HTTP 测试 · smoke-coarse · test-python | ✅ |
| 六边形目录骨架 · networks.md · RETIRE.md | ✅ |

---

## 9. 验收标准

- 业务侧对外实质端口：**8100 / 8101 / 8102 / 8103 / 8091 / 8092 / 8093**（+ gateway），无 12 个 Go 细端口。  
- 每个 `services/de-*` 可独立构建；`domain/` 无驱动 import。  
- 逻辑模块边界清晰（包 + schema + SERVICE.md 模块表）。  
- skill-runtime 无控制面 DSN；FE 经 gateway 联调通过。  

---

## 10. 建议首批 PR

| PR | 内容 |
|----|------|
| **PR-A** | 粗粒度目录骨架 + 现有服务归位（core→过渡壳；policy/audit 标记待并入 sys） |
| **PR-B** | Python 三服务 FastAPI |
| **PR-C** | de-sys + de-collab 切流 |
| **PR-D** | de-cap + de-workflow 切流并退役细端口 |

---

## 11. 非目标

- 为 platform/policy/audit/… 各起一个微服务端口  
- 将业务真相源迁到 Python  
- 把 agent-runtime / rag / skill-runtime 合并成一个 Python 进程  
- 首期上生产 SPIRE / runsc（目录与网络先就位即可）

---

## 12. 对照表（v3 细拆 → v4 粗粒度）

| 原拟微服务 | v4 归属 | 端口 |
|------------|---------|------|
| de-platform | **de-sys** | 8100 |
| de-policy | **de-sys** | 8100 |
| de-audit | **de-sys** | 8100 |
| de-ops | **de-sys** | 8100 |
| de-collab | **de-collab** | 8101 |
| de-employee | **de-collab** | 8101 |
| de-model | **de-cap** | 8102 |
| de-knowledge | **de-cap** | 8102 |
| de-memory | **de-cap** | 8102 |
| de-skill | **de-cap** | 8102 |
| de-channel | **de-cap** | 8102 |
| de-workflow | **de-workflow** | 8103 |
| de-agent-runtime / rag / skill-runtime | 各自独立 | 8091–8093 |
| de-gateway | 不变 | 8088 |
