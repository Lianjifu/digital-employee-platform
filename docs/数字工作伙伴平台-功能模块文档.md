# 数字工作伙伴 (Digital Work Partner) 平台 — 功能模块文档 v3.3

> 本文档配合「架构文档」「技术规格说明」「视觉设计规范」使用。  
> **v3.4（2026-08-20）**：对齐 `DE_ENV` 环境矩阵、硬删除 PersistDelete、技能岗位包按工作区安装态、平台工具 Harness 内置说明。
>
> **v3.3（2026-08-19）**：对齐 monolith 默认拓扑（de-app）、内置技能、侧栏与运营 live-aggregate。
> **v3.2（2026-08-17）**：对齐页面路由 `/partners`、侧栏四字文案、本仓后端联调。  
> **实现真相源**：[`README.md`](../README.md) · `App.tsx` · `features/role-nav/role-nav.ts` · `i18n` · `pages/*` · `backend/api/routes.md` · [`backend/deploy/topology-split.md`](../backend/deploy/topology-split.md)

---

## 0. 阅读说明

| 标记 | 含义 |
|---|---|
| ✅ 已实现 | 前端页面 + 本仓后端（或 Mock）可交互验证 |
| 🎯 目标 | 生产底座能力（Temporal/Milvus/真沙箱等），契约已在联调中模拟部分语义 |

控制型模块二级菜单统一顺序：

`概览或资产 → 接入与配置 → 验证与发布 → 运行与处置 → 治理与审计`

**文案约定**：侧栏主项四字对齐（如 **工作伙伴**）；产品全称「数字工作伙伴」用于页头与文档；审计员菜单为 **伙伴档案**。

---

## 1. 模块地图

### 1.1 导航 IA

```text
运营总览
协作：专家协作 → 任务中心
编排：工作伙伴 → 工作流程          ← 菜单四字；模块产品名仍为「数字工作伙伴」
能力：模型服务 → 知识中心 → 技能中心 → 记忆中心 → 消息渠道
账号菜单：工作区 · 平台设置（组织/身份/保留/集成/用量 + 访问控制/持续验证/审计）
```

审计员侧栏另含核查组：任务核查 / 协作记录 / **伙伴档案** / 流程版本 / 知识·技能·记忆·模型（只读）。

### 1.2 模块一览

| ID | 模块 | 路由 | 一句话 | 当前 | 目标后端（monolith → de-app） |
|---|---|---|---|---|---|
| M01 | 运营总览 | `/home` | 直播聚合 KPI 与运营入口 | ✅ | ops 模块 |
| M02 | 专家协作 | `/copilot` | 与岗位数字工作伙伴协同；证据与双签 | ✅ | collab + skill-runtime |
| M03 | 任务中心 | `/tasks` | 任务生命周期与人工治理 | ✅ | collab |
| M04 | 工作区 | `/workspaces` | 业务域空间与配额环境 | ✅ | sys/platform |
| M05 | 数字工作伙伴 | `/partners` | 岗位配置·装配·上岗·运营（菜单：工作伙伴） | ✅ | collab/employee |
| M06 | 工作流程 | `/workflows` | 模板·画布·版本·发布技能 | ✅ | de-workflow（可选） |
| M07 | 模型服务 | `/models` | 供应商·路由·治理·审计 | ✅ admin | cap/model |
| M08 | 知识中心 | `/knowledge` | 资产·加工·评测·图谱·引用 | ✅ | cap + RAG 按需 |
| M09 | 技能中心 | `/skills` | 清单·商店·集成·运行治理 | ✅ | cap + skill-runtime |
| M10 | 记忆中心 | `/memory` | 三层记忆·候选·策略审计 | ✅ admin/auditor | Go + Python |
| M11 | 消息渠道 | `/channels` | 接入·路由·健康·死信·审计 | ✅ admin | Go |
| M12 | 平台设置 | `/settings` | 租户与套餐；聚合三项治理 | ✅ admin | Go |
| M13 | 访问控制 | settings / 独立 | 授权·发布审批·SoD | ✅ | Go |
| M14 | 持续验证 | settings / 独立 | 零信任策略与事件 | ✅ | Go |
| M15 | 审计中心 | settings / 独立 | 只读追溯与脱敏导出 | ✅ | Go → OpenSearch |

> 旧「智能体市场」页与 `/agents`、`/digital-employees` 页面路由已移除；编排主路径仅为 `/partners`。底层 Agent 为执行内核，经 `/api/digital-employees` 装配引用；遗留 `/api/agents` 只读兼容。

---

## 2. M01 · 运营总览

**功能：** 平台总览仪表盘；展示与实体数据同源的直播聚合，不作演示数字充数。

### 当前实现

- KPI / 需关注 / 团队动态：以 `/api/digital-employees`、`/api/tasks` 等实体为准；`/api/home/kpis` 仅作 parity，不驱动主 KPI
- `/api/home/extra`、`/api/operations/overview`：`live-aggregate`（后端 `ops_aggregate`）；空工作区为 0 / 空列表
- 成本卡片：仅当计量来源为 `usage-meters` 时展示金额，否则 `—` /「暂无计量」
- 快捷入口：「工作伙伴」→ `/partners`；健康度深链 `/partners?tab=operations`；员工深链 `?employeeId=`

### 目标能力

- 持久 UsageMeters / Prometheus 刷新
- 异常红色高亮与健康度评分（有计量后）

### 依赖

- 任务中心、数字工作伙伴运行态、模型用量、合规/审计摘要

---

## 3. M02 · 专家协作

**功能：** 与已上岗数字工作伙伴协同处置；会话、工具、证据、审批与审计一体化。

### 当前实现

| 区域 | 能力 |
|---|---|
| 左栏会话记录 | 置顶/今天/更早分组；新会话；搜索；激活态与专家标签；删除走 `PersistDeleteSync`（含 messages / snapshots） |
| Composer | 产品层运行档：问答 / 方案 / 执行；推理档位（省 / 标准 / 深）；上下文占用；附件 / @ / 命令插入 |
| 中栏对话 | 消息流、ThoughtPanel、工作卡、工具调用、流式状态、人工交接、导出 |
| 右栏上下文 | 概览处置状态与治理 KPI；证据/任务/审批/审计 Tab；RAG 指标；岗位专家绑定 |
| 安全 | 生产写操作身份绑定受控双签；风险等级展示；管理员上岗/上架可自批，普通用户须审批 |

### 目标后端分工

| 层级 | 技术 |
|---|---|
| 对话路由、上下文、计费、双签校验 | Go |
| Agent 执行、RAG、工具/Skill 调用 | Python（LangGraph） |

### 核心特性

- 切换/绑定在岗岗位专家（非自由「市场试用」为主路径）
- 产品档位映射 ABI：问答/方案 → `investigate`，执行 → `execute`（写操作受策略与审核）
- 消息级可追溯：证据引用、关联任务、审批队列、审计流水
- 写动作二次确认 + 双签门禁
- 对话与工具调用写入审计关联 ID

### 依赖

- 强依赖：数字工作伙伴、知识、技能、模型路由  
- 联动：任务中心、消息渠道、持续验证、审计中心

---

## 4. M03 · 任务中心

**功能：** 将对话/事件任务化，看板与列表流转，支持人工治理与审计。

### 当前实现

- 生命周期看板 + 列表视图
- 状态流转、执行控制、人工治理
- 风险状态与审计追溯

### 核心特性（产品语义）

- Kanban：进行中 / 待复核 / 已完成 等
- 优先级与 SLA 语义（Mock）
- 与会话、审批、数字工作伙伴运行关联

### 依赖

- 专家协作（建任务）、工作流执行、审计中心

---

## 5. M04 · 工作区

**功能：** 业务域工作区控制面——资源、环境发布、配额成本；**不重复**建设权限/零信任/审计面。

### 当前实现

- 空间概览、资源目录、环境与发布、配额成本、空间设置
- 工作区切换影响后续请求上下文与缓存隔离

### 依赖

- 横切：访问控制、持续验证、审计中心、平台设置套餐配额

---

## 6. M05 · 数字工作伙伴

**功能：** 以**岗位**为边界的数字工作伙伴全生命周期：配置 → 能力装配 → 评测上岗 → 运行运营 → 证据。

**页面路由：** 仅 `/partners`（组件 `DigitalEmployees.tsx`）。侧栏文案 user/admin 为 **工作伙伴**，auditor 为 **伙伴档案**。

### 当前实现（主 Tab）

| Tab key | 中文 | 能力 |
|---|---|---|
| `catalog` | 专家目录 | 在岗/待上岗筛选、岗位与风险标签、岗位蓝图创建 |
| `roleSetup` | 岗位配置 | 档案、职责边界、人工接管与记忆策略契约 |
| `capabilities` | 能力装配 | 仅可选各能力中心**已准入/已发布**资产 |
| `release` | 上岗发布 | 评测门禁、双重审批、完整性检查 |
| `operations` | 运行管理 | 运行健康、交接与例外处置 |

### 深链与 API

| 项 | 约定 |
|---|---|
| 选中伙伴 | `?employeeId=` 或兼容 `?id=` |
| 切 Tab | `?tab=catalog\|roleSetup\|capabilities\|release\|operations` |
| 主 API | `/api/digital-employees` · `/overview` · `lifecycle` / `release` / `evaluate` / `configuration` / `evidence` … |
| 遗留 | `/api/agents*` 只读投影，前端主路径不依赖 |

### 核心原则

- 底层 Agent 为执行内核，由数字工作伙伴引用，不作侧栏一级「市场」主入口
- 模型/知识/技能/工作流/渠道仍由各自控制面发布；数字工作伙伴只引用版本并留存装配证据
- 长期记忆只形成知识候选，不直通权威知识

### 目标后端

- Go：生命周期状态机、发布门禁、权限（本仓 **de-app** monolith 已联调；coarse 下为 de-collab）  
- Python：评测与运行时 Agent 内核

### 依赖

- 能力：模型、知识、技能、记忆、工作流、渠道  
- 治理：访问控制（发布审批）、持续验证、审计

---

## 7. M06 · 工作流程

**功能：** 可视化编排业务执行路径，校验发布后可绑定为技能。

### 当前实现

| Tab | 能力 |
|---|---|
| 流程模板 | 平台内置：**办公通用**（制度问答/纪要/周报/请假等）+ 部门审批包；个人创建模板；连接器门禁与场景三联（知识·技能·流程） |
| 知识资产 | 目录、上传与复核、知识包；**办公开箱知识包**（手册/会议/写作/假勤/报销常识/IT 自助）冷启动发布 |
| 流程编排 | React Flow 画布 |
| 发布技能 | 受控发布，进入技能中心 |
| 运行记录 | 执行历史 |
| 辅助 | 版本 Diff、AI 辅助编排会话（`/workflows/orchestration`） |

### 目标后端

- Temporal Go Worker + Python Task  
- 失败切换、双签节点、审计落盘

### 依赖

- 知识检索、技能/工具、模型路由；发布后被数字工作伙伴装配

---

## 8. M07 · 模型服务

**功能：** 供应商接入、路由策略、发布回滚、故障演练与模型审计。

### 当前实现（admin）

| Tab | 能力 |
|---|---|
| 模型接入 | Provider、凭据引用（掩码）、连通性校验 |
| 模型路由 | 策略配置、校验/发布/回滚 |
| 模型治理 | 隔离与故障切换演练 |
| 模型审计 | 变更与调用相关审计 |

### 核心特性

- 跨工作区引用拒绝；已发布路由受依赖保护不可直接删  
- 受限数据出口与地域策略校验（Mock 语义）

### 目标

- Go 业务路由 + Python Provider SDK  
- 多 Provider 等级路由与预算护栏

### 依赖

- 被专家协作、工作流、知识评测调用；审计中心消费事件

---

## 9. M08 · 知识中心

**功能：** 企业知识资产全链路——入库、加工、检索评测、图谱与引用治理。

### 当前实现

| Tab | 能力 |
|---|---|
| 知识资产 | 目录、上传与复核、知识包；办公开箱知识包冷启动发布 |
| 接入加工 | 解析/切片/向量化流水线状态 |
| 检索评测 | 检索质量与评测 |
| 知识图谱 | 实体关系视图 |
| 引用治理 | 消费者绑定与引用审计 |

### 联动

- 记忆中心「知识候选」审核通过后进入知识草稿/资产深链

### 目标 RAG Pipeline

`解析 → 切片 → Embedding(BGE) → Milvus → Rerank → 引用回写`

### 依赖

- 模型服务（Embedding/LLM）；被专家协作与工作流检索调用

---

## 10. M09 · 技能中心

**功能：** Skill / MCP / Tool 与流程技能的安装、集成与运行治理。

### 当前实现

| Tab | 能力 |
|---|---|
| 平台工具 | Harness 内置工具目录 + **技能岗位包**（办公通用 / 通用 / 协作扩展 / 重依赖 / 全量） |
| 启用清单 | 已接入技能与状态 |
| 技能商店 | 可安装目录 |
| 流程技能 | 工作流发布产物 |
| 集成配置 | 连接与权限依赖 |
| 运行治理 | 健康、版本、治理事件 |

### 岗位包与平台工具

| 类型 | 说明 |
|------|------|
| **平台工具** | `knowledge.retrieve`、`memory.recall`、`skill.read`、`time.now` 等；Go Harness **启动即用**，不经岗位包安装 |
| **通用包** | 冷启动自动安装到各工作区 |
| **协作扩展 / 重依赖 / 全量** | `POST /api/skills/apply-pack/:id` 装到当前工作区；重依赖须审批单；列表接口返回 `installed` 态 |

### 三类能力（产品语义）

| 类型 | 说明 |
|---|---|
| Skill | 领域脚本化能力（如运维 CLI 封装） |
| MCP | 标准协议接入外部系统 |
| Tool | 工单/CMDB/通知等业务工具 |

### 依赖

- 工作流程（发布为技能）；数字工作伙伴装配；沙箱执行（目标 gVisor）
- 环境与清理：[`docs/环境与数据模式.md`](环境与数据模式.md)

---

## 11. M10 · 记忆中心

**功能：** 数字工作伙伴运行记忆分层治理，并向知识中心输送候选。

### 当前实现

| Tab / 能力 | 说明 |
|---|---|
| 记忆概览 | KPI、生命周期流、容量预警 |
| 短期 / 工作 / 长期 | 分层列表、状态与过期 |
| 知识候选 | 提炼候选；通过后跳转知识草稿 |
| 策略审计 | 岗位策略对照、审计流水 |
| 筛选 | 按数字工作伙伴过滤；来源深链 |

### 核心原则

- 运行记忆 ≠ 权威知识  
- 长期记忆按策略提炼为候选，人工/流程审核后入库

### 依赖

- 数字工作伙伴岗位策略；知识中心；审计

---

## 12. M11 · 消息渠道

**功能：** 企业消息通道接入、模板、投递路由与运行处置。

### 当前实现（admin）

| Tab | 能力 |
|---|---|
| 渠道接入 | 飞书/企微等接入与连通性 |
| 消息模板 | 模板管理（专业 ICON，无 emoji 依赖） |
| 投递路由 | 主备降级链、策略发布 |
| 运行健康 | SLO / 健康指标 |
| 失败处理 | 死信与重试 |
| 渠道审计 | 投递与变更审计 |

### 联动

- 平台设置「通知与告警」深链至消息渠道

### 依赖

- 任务/告警/审批事件触发；审计中心

---

## 13. M12 · 平台设置

**功能：** 租户级设置壳，聚合组织运营与三项安全治理入口。

### 当前实现八 Tab

| Tab | 能力 |
|---|---|
| 组织信息 | 租户档案、席位/数字工作伙伴配额条、通知订阅 Toggle |
| 身份认证 | SSO/MFA/会话说明卡 + 身份摘要条 |
| 数据保留 | 备份列表、立即备份、恢复语义 |
| 开发集成 | API Key、Webhook |
| 套餐用量 | 方案、月费、Token/席位/员工配额进度 |
| 访问控制 | 嵌入 M13 |
| 持续验证 | 嵌入 M14 |
| 审计中心 | 嵌入 M15 |

### 角色

- 仅 **admin** 进入设置写面  
- **auditor** 使用独立持续验证/审计路由，不进入设置配置

### 依赖

- 横切约束所有模块；计费配额约束数字工作伙伴与席位

---

## 14. M13 · 访问控制

**功能：** 成员授权、生产发布审批、职责分离。

### 当前实现

| 子 Tab | 能力 |
|---|---|
| 成员范围 | 授权列表、角色、工作区/环境范围、到期 |
| 发布审批 | 待审批生产发布 |
| 职责分离 | SoD 风险提示 |

### 角色矩阵（实现）

| 角色 | 能力摘要 |
|---|---|
| user | 运营与已授权能力读写；无 model/channel/access 写 |
| admin | 全量含发布审批与计费 |
| auditor | 广泛只读 + 审计导出；无写、不签发 |

---

## 15. M14 · 持续验证（零信任）

**功能：** 对身份、资源、环境、数据范围做持续策略决策。

### 当前实现

| 子 Tab | 能力 |
|---|---|
| 验证概览 | KPI（策略/阻断/审批/脱敏）+ 最近事件 |
| 访问策略 | 启用/停用（基线不可停）、版本 |
| 临时授权 | 授予/回收、到期失效 |
| 策略事件 | 命中说明与下一步指引 |

### 决策枚举

`allow` · `mask` · `approval_required` · `deny`

---

## 16. M15 · 审计中心

**功能：** 授权范围内只读追溯；脱敏证据包导出。

### 当前实现

- 审计事件列表（LIVE）、故事线（数字工作伙伴/任务/工作流）、合规复核、导出历史  
- 导出行为本身记审计；默认脱敏

---

## 17. 跨模块关键流程（对齐实现）

### 流程 A：专家协作处置（控制面）

```text
专家协作发问
  → 绑定在岗数字工作伙伴
  → 模型路由 + 知识/记忆检索
  → 技能/工具调用（策略评估）
  → 写操作：双签 + 持续验证
  → 任务中心跟进（可选）
  → 消息渠道通知（可选）
  → 审计中心关联 ID
```

### 流程 B：数字工作伙伴上岗

```text
岗位配置 → 能力装配（仅已发布资产）
  → 评测门禁 → 发布审批（访问控制）
  → 上岗运行 → 运行管理 / 记忆沉淀
  → 知识候选 → 知识中心审核入库
```

### 流程 C：渠道告警闭环（目标语义）

```text
消息渠道收告警 → 任务中心建单
  → 工作流/数字工作伙伴处置
  → 双签变更 → 渠道回执 → 审计
```

---

## 18. 登录与首次体验

| 能力 | 说明 |
|---|---|
| 登录 | 演示身份：管理员 / 业务构建者 / 审计员 |
| 向导 v2 | 能力接入 → 编排上岗 → 受控运营；预览导航与侧栏 IA 一致 |
| 主题/语言 | 中英词条契约；资产名不机翻 |

---

## 19. 模块依赖简图

```text
运营总览
   ├─ 协作：专家协作 ←→ 任务中心
   ├─ 编排：工作伙伴（/partners · 数字工作伙伴）→ 引用 ← 工作流程 / 模型 / 知识 / 技能 / 记忆 / 渠道
   └─ 治理：访问控制 / 持续验证 / 审计 ← 平台设置壳
工作区：提供作用域，不重复治理面
```

---

## 20. 版本记录

| 版本 | 日期 | 变更 |
|---|---|---|
| v3.0 | 2026-07-12 | P1–P11；OpenSearch + Milvus；Go/Python 双栈描述 |
| v3.1 | 2026-08-01 | 对齐现仓：专家协作/数字工作伙伴/记忆/消息渠道；平台设置八 Tab；去除侧栏「智能体市场」主路径 |
| v3.2 | 2026-08-17 | `/partners` 唯一页面路由；侧栏四字「工作伙伴」/「伙伴档案」；M01 live-aggregate；M05 Tab keys + 深链 + `/api/digital-employees`；承认本仓后端联调 |
| **v3.4** | **2026-08-20** | **`DE_ENV` 矩阵；硬删 PersistDelete；岗位包工作区安装态；平台工具 Harness 内置；见环境与数据模式.md** |
| v3.5 | 2026-08-20 | M02 Composer：问答/方案/执行 + 推理档；Postgres 仅 Docker（`ensure-docker-postgres.sh`）；根 README 产品截图 |

---

*功能模块文档 v3.5 · 与架构文档同步 · 模块能力以 `digital-employee-platform` 实现为准*
