# 默认：真实 API（de-core）
VITE_USE_MOCK=false
VITE_API_BASE=http://127.0.0.1:8080

# 若使用 Vite 代理（vite.config.ts server.proxy），可置空 base，仍请求 /api/*
# VITE_API_BASE=

# 仅本地无后端时启用 Mock（可选）
# VITE_USE_MOCK=true
